// const { sale } = require('../database/models');

// const createSale = async (salesInfo) => {
//   const { userId, sellerId, totalPrice, deliveryAddress, deliveryNumber } = salesInfo;

//   const newSale = await sale.create({ userId });
// };
